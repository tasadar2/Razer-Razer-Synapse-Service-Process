using Contract.Audio.ApplicationStreamsLib;
using Contract.Common;

namespace Synapse3.UserInteractive
{
    public delegate void OnApplicationStreamSet(Device device, ApplicationStream stream);
}
